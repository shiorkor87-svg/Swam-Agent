version: '3'

services:
  agent:
    image: your-agent-image
    ports:
      - "8000:8000"
    networks:
      - swam-network

  redis:
    image: redis:latest
    ports:
      - "6379:6379"
    networks:
      - swam-network

networks:
  swam-network:
    driver: bridge
